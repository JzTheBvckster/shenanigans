services:
  - type: web
    name: shenanigans
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeployTrigger: commit
    healthCheckPath: /
    envVars:
      - fromGroup: Firebase credentials
